🧹 Clean and optimize backend

- Remove obsolete documentation files (QUICKSTART, VALIDATION, SUMMARY, etc.)
- Add comprehensive seeding system for all modules
- Fix URL configurations (remove trailing slashes)
- Update Django and dependencies versions
- Add username field to User model with migration
- Fix admin and views configurations
- Add seed management commands for all apps
- Improve database initialization workflow
